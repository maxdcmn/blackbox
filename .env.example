# Blackbox Server Configuration
# Copy this file to .env and update with your values
# cp .env.example .env

# HuggingFace API Token (required for private models or rate-limited models)
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=hf_xxxxxxxxxxxxx

# Blackbox Server URL (for remote deployment)
# Use IP address or hostname for remote servers
# Examples:
#   - Local: http://localhost:6767
#   - Remote: http://192.168.1.100:6767
#   - Remote: http://server.example.com:6767
BLACKBOX_SERVER_URL=http://localhost:6767

# GPU Type (optional, auto-detected if not set)
# Options: A100, H100, L40, T4
# GPU_TYPE=A100

# Maximum concurrent models (optional, default: 3)
# MAX_CONCURRENT_MODELS=3

# Default port for deployments (optional, default: 8000)
# PORT=8000

# Starting port for batch deployments (optional, default: 8000)
# START_PORT=8000

# Port increment between models in batch deployments (optional, default: 1)
# PORT_INCREMENT=1

# Use sudo for Docker commands (optional, auto-detected if not set)
# Set to "true" to always use sudo, or leave unset for auto-detection
# USE_SUDO_DOCKER=true

# Log level (optional, default: INFO)
# Options: DEBUG, INFO, WARN, ERROR
# LOG_LEVEL=INFO
